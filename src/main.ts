export const greets = (name: string): string => `Hello, ${name}`
export const plus = (parm1: number, parm2: number): number => parm1 + parm2
export const multiply = (param1: number, param2: number): number => param1 * param2
